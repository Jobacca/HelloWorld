pipeline {
    agent { docker 'node:6.3' }
    stages {
        stage('Build') {
            steps {
                sh 'echo "build finished"'
            }
        }
        stage('Test') {
            steps {
                sh 'echo "test finished"'
            }
        }
    }
}
