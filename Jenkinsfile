pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "build finished"
            }
        }
        stage('Test') {
            steps {
                echo "test finished"
            }
        }
    }
}
